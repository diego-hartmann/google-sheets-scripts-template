{
  "name": "apps-scripts-google-sheet",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=dist/code.gs --format=iife",
    "push": "clasp push",
    "format": "prettier --write .",
    "deploy": "npm run build && npm run push"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "prettier": "^3.6.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
